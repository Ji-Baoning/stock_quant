# RUNBOOK 收敛记录（2026-09-14）

## 结论

根目录的 [`RUNBOOK.md`](../../RUNBOOK.md) 是唯一操作入口。已删除
`project/RUNBOOK.md`；不保留重定向或副本，以避免同一操作出现两份权威说明。

## 差异处置

| 原 `project/RUNBOOK.md` 章节 | 处置 | 根文档目标章节/理由 |
| --- | --- | --- |
| 阶段 0（准备、凭据） | 已在根文档 | `RUNBOOK.md` 阶段 0；根文档说明启动方显式导出 relay 环境，避免误称程序自动读取 `.env`。 |
| 阶段 1–3（离线检查、bootstrap、小窗口） | 已在根文档 | 阶段 1–3；命令保留显式 `--root` 语义。 |
| 阶段 4（relay 发布与质量门） | 已在根文档并更新 | 阶段 4；根文档要求 `TUSHARE_TRANSPORT=relay` 并使用 `--root project`。 |
| 阶段 4.5（旧数据验收） | 过期，不迁移 | 根文档阶段 5b 是当前 `prepare`/人工编辑/`publish` 的不可变验收流程；旧副本的 `acceptance confirm` 不是当前入口。 |
| 阶段 5（研究）与阶段 6（报表） | 已在根文档并扩展 | 根文档阶段 6–7；包含当前身份、复现和验收门禁。 |
| 已知边界 | 已在根文档并扩展 | 根文档“已知边界”；以当前数据源和验收约束为准。 |

## 维护规则

- 操作命令只维护在根 `RUNBOOK.md`；运行时项目根不是仓库根时，命令必须显式给出 `--root project`，或先明确 `cd project`。
- 变更发布、验收或诊断流程时，更新根手册和对应的日期化操作证据；不得恢复项目内副本。
- `project/` 下辅助脚本的状态和替代入口见 [`project/SCRIPTS.md`](../../project/SCRIPTS.md)。
