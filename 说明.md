# 说明：术语、数据与关键参数

这是一份**阅读辅助**，用于快速理解本项目文档和运行日志里出现的专业词汇、
用到的数据及其来源、关键参数的含义。它不是规范、不是契约，也不构成投资
建议。任何术语的精确定义以其权威来源为准（见文末「去哪里看权威定义」）。

项目定位：A 股量化研究的**可复现工程链路验证**（一期 MVP）。目标是证明
「供应方数据 → 质量门禁 → 时点成分 → 冻结实验 → 时序回测 → 组合构建 →
报告」这条链路能跑通且可审计，**不**声称策略有效性、盈利能力或实盘可用性。
对外发布的指标是工程证据，不是投资结论。

---

## 一、A 股市场与交易制度术语

| 术语 | 含义 |
| --- | --- |
| 复权 | 把分红送股造成的价格跳空还原后的价格序列。本项目用 `adjusted_bar` 表（`adjustment=internal_total_return_v1`），由未复权收盘价 + 已核实的现金分红/送股/转增事件推导，口径接近「全收益」。 |
| 公司行为 | 分红、送股、转增、配股等会改变股本或价格的事件（corporate action）。未经核实的事件进入 `corporate_action_quarantine` 隔离区，只留证据不做使用。 |
| 停牌 / 复牌 | 证券暂停交易。停牌期间由供应商 `pre_close` 链派生出「carry 行」（`tushare_suspend`）：它延续该供应商的价格序列，但**不贡献真实收益**，也不计入风险观测数。 |
| 涨跌幅限制 | 单日价格波动的上下限。按板块和证券状态分档（主板 ±10%、`ST`/`*ST` ±5%、创业板注册制后 ±20%、科创板 ±20%），规则带**生效日期**，执行代码不内嵌比例。 |
| 注册制新股无涨跌幅 | 创业板/科创板/全面注册制后的主板新股，上市后**前 5 个交易日**不设涨跌幅。 |
| `price_tick` | 最小报价变动单位，本项目取 `0.01` 元。 |
| 整手 / `lot_size` | A 股按手交易，1 手 = 100 股。下单量必须向下取整到整手，不足一手的差额被抑制（`below_one_lot`）。 |
| 印花税 | 官方费率，**卖出单边**：1‰，2023-08-28 起减半为 0.5‰。 |
| 佣金 / 最低佣金 | 操作者申报假设（非官方）：0.3‰、单笔最低 ¥5。实测配置下每笔成交都命中 ¥5 最低值，故佣金费率对结果无影响。 |
| 过户费 | 官方双向收费，本项目 **不建模**（成本模型无该字段），其影响已在扩展窗口配置中量化记录。 |
| 滑点 | 成交价相对决策价的偏移假设，操作者申报值 0.1%。 |

---

## 二、量化研究术语

| 术语 | 含义 |
| --- | --- |
| 因子 / 信号 | 用于排序选股的数值。本期唯一因子是 `momentum_60d`（60 交易日动量）。 |
| 动量 | `adjusted_close[t] / adjusted_close[t-60] - 1`，即过去 60 个交易日的复权收益率；取值为正表示前期涨过。 |
| 信号日 | 产生信号的交易日。本因子按 **周频**（`frequency = "weekly"`）出信号，调仓也在信号日决策。 |
| 预热（warmup） | 每个 fold 正式开始前只用于**构造因子历史**的区间，绝不产生订单或收益。 |
| 样本外（OOS） | 未参与任何参数选择、只用于验证的时间段。本项目的 walk-forward 把样本外切成年度的、互不重叠的 fold。 |
| 走查 / Walk-Forward | 用同一个**冻结**策略，在连续的年度样本外区间上滚动验证稳定性；它**不**选参数、不比较频率、不挑成本情景、不丢弃表现差的 fold。 |
| fold（折） | 一个独立的样本外区间。每个 fold 独立账户、相同初始资金，fold 边界处组合成员状态重置。 |
| 基线 / 挑战者 | 一次性策略对比的两侧：基线为等权 Top-10，挑战者为缓冲式风险加权。 |
| holdout（留出集）一次性消耗 | 样本外证据只要被用过一次即「消耗」，**不可恢复**。换宇宙版本、改参数、上一次结果失败都不能把已消耗的历史重新变成「未见过的」。 |
| 时点（PIT, Point-in-Time） | 决策只能使用当时可得的信息。成分股、信号、资格判定都必须按决策日当天的事实，**不允许前视**。 |
| 幸存者偏差 | 只统计至今仍上市的股票导致的高估。本项目的可交易宇宙由人工挑选的在市股票构成，**该偏差仍然存在**，定义文件里已明示。 |
| 换手（turnover） | 组合持仓的变动幅度。本期拆为「成员变化换手」与「连续持仓再平衡换手」两项分别统计。 |
| 最大回撤 | 净值从峰值回落的最大幅度。本项目**只**按每个 fold 自身净值计算，跨 fold 的全局回撤与 Calmar 比率被**禁止**，不出现在任何产物中。 |
| 夏普比率 | 样本统计量（`ddof=1`，无风险利率取 0），年化用 252 个交易日。未定义的值写成 JSON `null`，绝不填 0。 |
| 年化收益 | `prod(1+r)^(252/N) - 1`；累计收益为 `prod(1+r) - 1`。 |

---

## 三、工程与治理术语

| 术语 | 含义 |
| --- | --- |
| 项目根 / `--root` | 运行期数据的落点，必须显式给出且自带 `configs/project.yml`、`sources.yml`、`costs.yml`。**没有**回退到仓库根或模板目录——根不对就报错，不会读错数据。 |
| 数据集版本 | 一次 `data update` 发布的、内容寻址且**不可变**的数据快照。相同输入重发是空操作；内容变了就发布新版本，不改旧版本。 |
| `CURRENT` | 指向最新的、已通过全部门禁的数据集版本。一次运行在**固定版本那一刻**解析一次，此后全程只读该版本，不会中途跟着 `CURRENT` 走。 |
| 内容寻址 | 产物 id = 其内容的哈希。相同冻结输入跑两次，得到相同 id 和逐字节相同的产物。运行期元数据（run id、时间戳、主机、pid、进程数、输出路径）**不进入**身份哈希。 |
| 质量门禁 | `QualityReport` 的判定层。阻断发布的情况：价格非正、违反 schema、与主源冲突、取数失败、必需源不可用；可选校验失败与尽力而为的公司行为缺口只产生 WARNING。 |
| 真实数据验收 | 针对**某个已发布数据集版本**的、操作者签名且内容寻址的结论（`policy_version=real-data-v1`），判定其供应质量是否达标。它是**数据供应**门禁，不是策略结论。 |
| `ACCEPTED` / `REJECTED` | 验收的两种记录。REJECTED 会**先持久化再退出**（留证据）；没有有效 ACCEPTED 记录时，正式研究在算任何因子之前就失败。 |
| `CURRENT_ACCEPTED` | 规格里的占位符：运行时解析为该数据集最新的有效 ACCEPTED 记录并逐次复验；冻结进实验目录的规格永远携带解析后的 64 位十六进制 id。 |
| 信任模式 | `research`（正式，需验收）与 `engineering`（工程诊断）。工程诊断不读验收记录，恒记为 `UNVERIFIED`，绩效可信度恒为 `UNTRUSTED`，**永远不能**发布 ACCEPTED 实验或正式稳定性结论。 |
| 冻结 / freeze | 解析版本、验收、提交号与信任档，把规格与三份研究快照（策略 / 实验 / 数据环境）钉死，此后不可改写。 |
| `experiment_id` | 冻结规格 + 三份快照 + 代码提交号的内容哈希（identity scheme v2）。 |
| `portfolio_rule_version` | 组合规则规范化 JSON 的 SHA-256，不是手写标签。任何显式参数修改 = 新的实验身份。 |
| 成分表证据 | 时点宇宙不是「一串代码」，而是带证据的成员事实：官方快照 → 绑定哈希的事实表 → 随数据集发布 → 定义文件钉住表哈希。数量不对、边界含糊、证据缺失都**停止工作**，没有绕过开关。 |
| `universe_id` / `universe_version` | 宇宙定义的名字 / 该定义内容的哈希。 |
| `membership_table_sha256` | 已发布成员表的内容哈希，定义文件与数据集必须逐位一致。 |
| `evidence_summary_sha256` | 证据摘要的哈希，与上一条共同构成完整宇宙身份四元组。 |
| 抑制 vs 拒单 | **抑制**（`within_rebalance_band`、`below_one_lot`）是下单**之前**的组合决策，记录在 `rebalance_decisions.parquet`；**拒单**是执行层拒绝，记在 `rejections.parquet`；风险输入无效（`risk_invalid`）是第三件事，记在构建审计里。三者不可混同。 |
| 稳定性结论 | `STABLE` / `UNSTABLE` / `INCONCLUSIVE` / `FAILED`。`FAILED` 表示流程或完整性出问题（退出码非零，结论为 `null`，**不能**降级成 INCONCLUSIVE）；`INCONCLUSIVE` 是「流程有效但证据不足」，与策略好坏无关。 |
| 无前视 / no look-ahead | 不跟随已固定版本之后的 `CURRENT`、不向前延展预热窗口、不用决策日之后的 bar 稳定早期因子值。 |
| 脱敏（redaction） | 日志与错误文本写盘前必经脱敏路径；token 只从环境变量读（如 `TUSHARE_TOKEN`），不入库、不入配置、不入报告。 |

---

## 四、项目用到的数据与数据源

### 4.1 数据源总览

| 源 | 角色 | 当前状态 | 提供什么 |
| --- | --- | --- | --- |
| **tushare** | **必需主源** | `enabled: true` | 个股未复权日线（`daily`）、股票列表（`stock_basic`）、交易日历（`trade_cal`）、指数日线（`index_daily`）。 |
| **akshare** | **必需参照源** | `enabled: true` | 每个基准指数一条历史行情（`index_history`），以及尽力而为的公司行为核对（cninfo / eastmoney）。 |
| **baostock** | **可选校验源** | `enabled: true` | 另一份未复权日线，仅用于交叉校验，另含供应商自己的停牌日行（`tradestatus=0`）。2026-09-05 起其数据服务不可达，2026-09-19 恢复后重新启用。 |
| tushare relay / proxy | **不是独立源**，是 tushare 兼容的**传输层** | 依环境变量启用 | 当 `TUSHARE_PROXY_URL` + `TUSHARE_PROXY_KEY` 存在时，tushare 适配器改走中转（分页窗口 + 瞬时重试），官方 SDK 作为回退。它**只搬运数据，永远不作证据源**。 |
| 官方公告与快照（人工获取、离线入库） | **证据源** | 已固化 | CSI 成分公告、tushare `index_weight` 月度成分快照——时点宇宙的证据链原件，存于 `data/raw/csi/`，永不提交。 |

akshare 的 `index_history` 实际由上游厂商回答，可能是东方财富（`stock_zh_index_daily_em`/`hist_em`）、新浪（`stock_zh_index_daily`）或腾讯（`stock_zh_index_daily_tx`）——回退链**运行时**可能切换，所以原始存档记录的是「厂商 + 接口」而不只是厂商标识。

### 4.2 已发布数据集里的表

一个数据集版本是这些表的集合（`data/standardized/<version>/`）：

| 表 | 数据来源 | 作用 |
| --- | --- | --- |
| `daily_bar` | tushare `daily`（主源）+ akshare 基准指数行 + 由 tushare `pre_close` 链派生的停牌 carry 行 | **未复权**日线。执行、估值、涨跌停检查的**唯一**价格口径。 |
| `adjusted_bar` | **派生，非外部源**：由 `daily_bar` 未复权收盘 + 已核实公司行为推导（`internal_total_return_v1`） | **只作因子证据**，绝不用于下单或估值。 |
| `security_master` | tushare `stock_basic` | 上市事实：`list_date` / `delist_date` / `list_status`。宇宙标签来自 `configs/universe.yml`，不来自供应商。 |
| `security_master_coverage` | 同上 | 上市事实的覆盖审计（哪个符号、哪一段被成功覆盖）。 |
| `trading_calendar` | tushare relay `trade_cal` | 开市日与 `pretrade_date` 链。沪、深两所**分别请求后逐日比对**，不一致即阻断。更新终点取自已发布日历，不看墙上时钟。 |
| `corporate_action` | akshare：cninfo 分红送转（`stock_dividend_cninfo`）、eastmoney 分红送转（`stock_fhps_detail_em`）、cninfo 配股（`stock_allotment_cninfo`）；跨源冲突由 `configs/corporate_action_reviews.yml` 人工裁决 | 复权推导的输入事件（现金分红、送股、转增、配股）。 |
| `corporate_action_quarantine` | 同上，未通过核实/跨源冲突的记录 | **隔离区**：留证据、不参与推导。公司行为核对是尽力而为，缺口只产生 WARNING，不阻断发布。 |
| `corporate_action_coverage` | 同上 | 公司行为抓取的覆盖审计。 |
| `universe_membership` | 官方成分快照（CSI 公告／tushare `index_weight` 月度快照），经 `data index-membership prepare` 绑定快照哈希与来源文档哈希 | **时点成分事实**。以「天」为粒度记录成员，绝不重算或推断。 |

### 4.3 数据集之外的输入

| 输入 | 位置 | 作用 |
| --- | --- | --- |
| `project.yml` | `configs/` | 全局窗口、初始资金、基准符号。 |
| `costs.yml` | `configs/` | 成本情景费率表（佣金/印花税/滑点），带生效日。 |
| `trading_rules.yml` | `configs/` | 涨跌幅与新股无涨跌幅制度，带生效日。 |
| `universe.yml` | `configs/` | 追踪宇宙（与 `security_master` 逐符号一致是硬契约）。 |
| `universes/*.yml` | `configs/universes/` | 宇宙**定义**：钉住成员表内容哈希、证据摘要哈希与覆盖窗口。 |
| `corporate_action_reviews.yml` | `configs/` | 人工裁决记录：跨源冲突时选哪个源、为什么。 |
| 证据原件 | `data/raw/**` | 供应商原始响应与官方公告，按内容哈希存档，永不提交仓库。 |
| `TUSHARE_TOKEN` | 环境变量 | 唯一凭据入口。**不入代码、不入配置、不入日志**。 |

### 4.4 源的角色纪律

- **主源对每个字段保持权威**：跨源冲突被记录为 ERROR 证据，**从不取平均**。
- **必需源失败 → 阻断发布**：`CURRENT` 不变，不产生新版本。**可选源失败 → WARNING**，照常发布。
- **数据源 ≠ 证据源**：中转层可以换，官方公告/快照不能换。宇宙与公司行为的证据链始终锚在官方文件及其 SHA-256 上。
- 每个源写进自己的原始树（`data/raw/tushare/`、`akshare/`、`baostock/`、`csi/`），不覆盖他源记录。

---

## 五、关键参数速查

### 5.1 全局（`configs/project.yml`）

| 参数 | 当前值 | 说明 |
| --- | --- | --- |
| `start_date` / `end_date` | 2021-01-01 / 2026-08-30 | 全局窗口。注意实验规格自带 `date_range`，两者不必相同。 |
| `initial_cash` | 1,000,000 | 每个 fold 独立账户的相同初始资金。 |
| `benchmark_symbols` | `000300.SH`, `000905.SH` | 沪深 300 / 中证 500，用于对比基准。 |
| `publication_time` | `15:00` | 仅记录用途；更新终点取自已发布交易日历，不看时钟。 |

### 5.2 成本情景（`configs/costs.yml`）

三个情景预声明，**无偏好情景**，稳定性结论对三者取合取：

| 情景 | 佣金 | 最低佣金 | 印花税（卖出） | 滑点 |
| --- | --- | --- | --- | --- |
| `zero_cost` | 0 | 0 | 0 | 0 |
| `commission_tax` | 0.3‰ | ¥5 | 1‰ → 0.5‰（2023-08-28 起） | 0 |
| `full_cost` | 0.3‰ | ¥5 | 1‰ → 0.5‰（2023-08-28 起） | 0.1% |

费率表按**生效日**解析（取生效日不晚于交易日的最新一条），因此只声明真正发生变化的日期。

### 5.3 涨跌幅制度（`configs/trading_rules.yml`）

| 板块 | 状态 | 生效日 | 幅度 |
| --- | --- | --- | --- |
| 沪/深主板 | NORMAL | 1996-12-16 | ±10% |
| 沪/深主板 | `ST` / `*ST` | 1998-04-27 | ±5% |
| 创业板 | NORMAL | 2009-10-30 | ±10% |
| 创业板 | NORMAL | 2020-08-24 | ±20% |
| 科创板 | NORMAL | 2019-07-22 | ±20% |

无涨跌幅：科创板 2019-07-22 起、创业板 2020-08-24 起、主板 2023-04-10 起，新股上市**前 5 个交易日**。

### 5.4 因子 `momentum_60d` v2

| 参数 | 值 | 说明 |
| --- | --- | --- |
| `lookback` | 60 | 回看 60 个交易日。 |
| 最少观测 | 61 | 信号日及之前至少 61 个观测点。 |
| 窗口内质量 | 无 `ERROR` 行 | 回看窗口内出现 ERROR 质量行即作废。 |
| 上市时长（seasoning） | ≥ 120 个交易日 | 次新股剔除。 |
| 频率 | 周频 | 信号按周产生。 |

### 5.5 组合规则

**`top_n_equal_weight`（等权基线）**：`top_n = 10`，`lot_size = 100`。

**`buffered_risk_weighted`（缓冲式风险加权，`momentum_60d.yml` 当前选中的规则）**：

| 参数 | 默认值 | 含义 |
| --- | --- | --- |
| `target_count` | 10 | 目标持仓数量。 |
| `entry_rank` | 10 | 新进成员只看风险合规排名前 10 名。 |
| `hold_rank` | 15 | 已在持仓的成员可保留到前 15 名。 |
| `risk_lookback_days` | 60 | 风险（波动率）估计回看窗口。 |
| `min_risk_observations` | 40 | 至少 40 个真实收盘观测，否则该标的风险输入无效并被剔除（剔除不导致 fold 失败）。 |
| `volatility_floor_annualized` | 0.10 | 年化波动率下限，防止权重发散。 |
| `max_single_weight` | 0.15 | 单票权重上限。 |
| `rebalance_band_absolute` | 0.02 | 再平衡带宽：连续持仓权重偏差 **≥ 2%** 才调仓，严格小于 2% 被抑制。 |
| `gross_exposure` | 1.00 | 总仓位上限，仅多头、无杠杆。 |
| `weight_quantum` | 1e-12 | 权重向下的量化粒度；余量按代码升序回补，无法分配的部分留作现金，保证 `sum(target_weight) + cash_weight == 1.00`。 |

约束：`entry_rank <= hold_rank`、`target_count == entry_rank`、`min_risk_observations <= risk_lookback_days`，且全部参数被冻结身份绑定。

### 5.6 Walk-Forward 与稳定性（`stability-v1`）

| 参数 | 值 | 含义 |
| --- | --- | --- |
| `warmup_years` | 3（整年） | 预热至少 3 个日历年。 |
| `min_warmup_trading_days` | 756 | 预热至少 756 个确认交易日；单年交易日不足时按整年向前扩展。 |
| `required_stable_history_days_before_s` | 60 | 首个样本外日之前还需 60 个稳定历史交易日。 |
| `oos_months` / `step_months` | 12 / 12 | 12 个月不重叠样本外窗口，1 月 1 日锚点。 |
| `calendar_anchor` | `jan_1` | 窗口锚点。 |
| `account_reset` | true | 每个 fold 账户重置。 |
| `aggregate` | `oos_only` | 只聚合样本外收益。 |
| `global_drawdown_aggregation` | `forbidden` | 跨 fold 回撤与 Calmar 被禁止。 |
| `minimum_executed_folds` | 5 | 少于 5 个已执行 fold → `INCONCLUSIVE`。 |
| `minimum_positive_fold_ratio` | 0.60 | 正收益 fold 占比 ≥ 60%。 |
| `worst_fold_calendar_return_floor` | -0.10 | 最差 fold 日历收益 **严格大于** -10%。 |
| `annualization_sessions` | 252 | 年化交易日数。 |
| `risk_free_rate` | 0.0 | 无风险利率取 0。 |

结论对所有成本情景**独立判定后取合取**：全部通过 → `STABLE`；任一不通过 → `UNSTABLE`。一个 fold 只因全市场休市而记为 `skipped_not_tradeable`，且必须有覆盖整个样本外窗口的版本化休市证据才合法；这种跳过使结果落为 `INCONCLUSIVE`，而非通过。

---

## 六、去哪里看权威定义

本文件只是索引式辅助，冲突时以下列来源为准：

| 想知道什么 | 看哪里 |
| --- | --- |
| 项目定位、模块职责、技术选型 | `docs/architecture/overview.md` |
| 一个产物从取数到发布的完整生命周期 | `docs/architecture/data-flow.md` |
| 不可违反的约束（前视、不可变、门禁、价格口径等） | `docs/architecture/invariants.md` |
| 某个决策为什么这样定、否决了哪些方案 | `docs/adr/DECISIONS_INDEX.md` |
| 命令怎么跑 | `RUNBOOK.md` |
| 长期业务背景与当前状态 | `PROJECT_MEMORY.md` |
| 用户向总览 | `README.md` |

**注**：本项目所有已发布的性能数字均为工程证据，绩效可信度为 `UNTRUSTED`（除非另有说明），不构成投资建议。
